React + Vite
This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.
React Router DOM → Navigation between pages (Home, Products, Cart, etc.).
Axios → Making API requests to the backend.
Redux Toolkit → Managing global state (cart, user, wishlist).
React Icons → Professional icons without downloading images.
React Hot Toast → Beautiful success/error notifications.